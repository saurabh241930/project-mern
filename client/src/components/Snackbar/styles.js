import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
    Snackbar:{
        backgroundColor:'rgb(255, 165, 0)',
        textAlign:'centre'
    },
    qualityName:{
        color:'rgb(255, 165, 0)'
    }
}));
